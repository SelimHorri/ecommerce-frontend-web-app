import { UserDtoCollectionResponse } from './user-dto-collection-response';

describe('UserDtoCollectionResponse', () => {
  it('should create an instance', () => {
    expect(new UserDtoCollectionResponse()).toBeTruthy();
  });
});
